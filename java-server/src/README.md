# Directory Purpose
Java server source root.

## What Lives Here
- `main/` application source set.

## How It Connects
- Standard Gradle source set consumed by `java-server/build.gradle`.

## Runtime Role (LV1/LV2/Server)
- Container for server code that powers LV2 execution.

## Maintenance Notes
- Add tests under conventional sibling source sets if introduced.
