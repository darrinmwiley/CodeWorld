# Directory Purpose
Top-level Java package root for server implementation.

## What Lives Here
- `com/` package namespace.

## How It Connects
- Houses codeworld backend classes and generated package references.

## Runtime Role (LV1/LV2/Server)
- Java source root compiled into server app.
