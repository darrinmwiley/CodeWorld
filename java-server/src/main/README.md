# Directory Purpose
Main production source set for Java server.

## What Lives Here
- `java/` package tree.

## How It Connects
- Built and launched by Gradle `application` plugin (`CodeWorldServer` main class).

## Runtime Role (LV1/LV2/Server)
- Entry source set for backend runtime.
