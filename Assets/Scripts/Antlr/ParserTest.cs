using Antlr4.Runtime;

class ParserTest
{
    /*static void Main(string[] args)
    {
        var input = new AntlrInputStream("public class MyClass { }}");
        var lexer = new CSharpLexer(input);
        var tokens = new CommonTokenStream(lexer);
        var parser = new CSharpParser(tokens);

        // Start parsing from the compilation_unit rule (or the starting rule of your choice)
        var tree = parser.compilation_unit();

        // Optionally, print the parse tree
        Console.WriteLine(tree.ToStringTree(parser));
    }*/
}
